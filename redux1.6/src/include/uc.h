[Number of equation is stored in an unused field of the 
 critical pair data structure]
const ENUM(E)=CPLOI(E).

[macro for extracting CP right origin information;
 this one is missing in tp.h]
const CPROI(CP)=FIFTH(CP).

[maximum completion rounds counter for unfailing completion]
global MAXSTP.
safe   MAXSTP.
